export interface BoardSimple {
    items: {
        code: number,
        categoryName: string,
        title: string,
        profileUrl: string,
        profileName: string,
        uptime: string,
        view: number,
        share: number,
        like: number
    }
}